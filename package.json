{
  "name": "patch-notes-from-another-universe",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "wrangler deploy --dry-run",
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "wrangler": "^4.129.0"
  }
}
